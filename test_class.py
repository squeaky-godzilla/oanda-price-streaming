class tester():
    def __init__(self, text, number):
        self.text = text
        self.number = number

    def print_things(text, number):
        print('this is the text: %s and this is the number %i' % (text,number))

import pdb; pdb.set_trace()